<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
    <link rel="stylesheet" href="login.css">
</head>

<body>
    <button class="toggle-button" id="toggleButton">{{list}}</button>
    <div class="noteimp">
        <label for="">Registration Process:</label>
        <ol>
            <li>Only one user can register at the start of the application.</li>
            <li>Once a user successfully registers, no other person can register.</li>
        </ol>
        <label for="">Login Process:</label>
        <p>1)After the first successful registration, only login functionality is available.</p>
    </div>

    <div class="main">
        <img src="images.jpg" alt="">

        <div class="all1">
            <div>
                <form action="/Reg" method="post">
                    <div class="username">
                        <label for="">👲</label>
                        <input type="text" name="name" placeholder="Enter Username">
                    </div>
                    <div class="password">
                        <label for="">🔑</label>
                        <input type="password" id="password" placeholder="Enter Password">
                    </div>
                    <div class="password">
                        <label for="">🔑</label>
                        <input type="password" id="confirm_password" name="password" placeholder="Confirm Password">

                    </div>
                    <div class="match" style="text-align: center;margin-top:5px">
                        <span id="error-message" class="error-message"
                            style="color:rgb(20, 16, 101);font-size:18px;font-weight:bold;margin:auto;"></span>
                    </div>
                    <div class="btn1">
                        <button type="submit" id="registerButton" class="login">REGISTER</button>
                    </div>
                </form>
                <div class="register1">
                    <a href="/">Click Here To Login</a>
                </div>
            </div>
        </div>
    </div>

    <script>
        var toggleButton = document.getElementById('toggleButton');
        // Function to show the success message
        function showSuccessMessage() {
            toggleButton.style.display = 'block';
            setTimeout(function () {
                toggleButton.style.display = 'none';
            }, 15000); // Hide the message after 3 seconds (3000 milliseconds)
        }

        const passwordInput = document.getElementById('password');
        const confirmPasswordInput = document.getElementById('confirm_password');
        const errorMessage = document.getElementById('error-message');
        const registerButton = document.getElementById('registerButton');

        confirmPasswordInput.addEventListener('input', function () {
            if (passwordInput.value !== confirmPasswordInput.value) {
                errorMessage.textContent = 'Passwords do not match';
                registerButton.disabled = true;
            } else {
                errorMessage.textContent = '';
                registerButton.disabled = false;
            }
        });
    </script>
    {{{script}}}
</body>

</html>